export default interface Closet {
    id: string; 
    user: string; // user's ID to whom this closet belongs
    name: string; // Name of the closet (e.g., "Winter Wardrobe")
    createdAt: string;
    updatedAt: string; 
  }
  